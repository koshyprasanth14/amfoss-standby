Practice Questions 11

1.
assert spam < 10, "mor number giv"

2.
assert eggs.lower() == bacon.lower(), "meh them sem"

3.
assert False, "how to keep an idiot busy"

4.
import logging, logging.basicConfig()

5.
import logging, logging.basicConfig(filename='programLog.txt', level=logging.DEBUG)


6.
debug, info, warning, error, critical

7.
logging.disable(logging.CRITICAL)

8.
when we finish the code,we might want to remove the print() messages, and using log messages makes it a lot easier as it can be done in one line.

9.
Clicking the Step In button will cause the debugger to execute the next line of code and then pause again.Clicking the Step Over button will execute the next line of code, similar to the Step In button. However, if the next line of code is a function call, the Step Over button will “step over” the code in the function. Clicking the Step Out button will cause the debugger to execute lines of code at full speed until it returns from the current function. 

10.
if there are no more breakpoints, the code will run until all of it is executed.

11.
A breakpoint can be set on a specific line of code and forces the debugger to pause whenever the program execution reaches that line.

12
to set a breakpoint, we can click on the line number in the file editor to cause a red dot to appear, making it a breakpoint.

13.
[debug_coin](./debug.py)

Practice Questions 8

1.
No

2.
as the name of the extenral libary is a little long, importing it as pyip lets us use pyip as the name.

3.
inputInt() accepts integers , while inputFloat() accepts integers or float.

4.
we can use 
inputInt(min=0, max =99)

5.
raw strings are passed, and the element are allowed and not allowed in allowRegexes and blockRegexes keyword arguments respectively.

6.
If the user answers incorrectly more than 3 times, it raises a RetryLimitException exception, while repeating that blank values are not allowed.

7.
Since a default value is set, the value will be set to 'hello'

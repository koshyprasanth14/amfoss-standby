Practice Questions 4

*1.*
Square brackets can be used to show that the data will be a list.Just a [] denotes an empty list.

*2.*
spam = [2, 4, 6, 8, 10]
spam[2] = 'hello'

*3.*
d

*4.*
d

*5.*
['a', 'b']

*6.*
1

*7.*
[3.14, 'cat', 11, 'cat', True, 99]

*8.*
[3.14, 11, 'cat', True]

*9.*
operator '+' is used to concantate lists and '*' is used to replicate strings

*10.*
append is used to add an element at the end of the list while insert is used to add an element to a specific index.

*11.*
pop and remove can be used, pop is used to remove a specific value, while remove is used to remove a value with a specific index.

*12.*
both lists and strigs can be indexed and sliced, and they can be iterated over using loops.

*13.*
the contents in a list can be modifed but the contents of a tuple cannot ebe modified.

*14.*
we can use the tuple() function.

*15.*
we can use the list() and tuple() functions. say we have a list named x, we can use tuple(x) to make a tuple. if x were a tuple, we can use list(x) to make a list.

*16.*
[Comma code](./comma.py)

*17.*
[Character picture grid](./grid.py)

